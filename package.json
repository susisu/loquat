{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "lint": "lerna run --concurrency 1 --no-bail lint -- --color",
    "lint:fast": "lerna run lint",
    "test": "lerna run --concurrency 1 --no-bail test -- --colors",
    "test:fast": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
