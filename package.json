{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "lint": "lerna run --concurrency 1 --no-bail lint",
    "lint:fast": "lerna run lint",
    "test": "lerna run --concurrency 1 --no-bail test",
    "test:fast": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
