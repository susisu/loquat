{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "lint": "lerna run --ignore loquat lint",
    "test": "lerna run --ignore loquat test"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
