{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run --ignore loquat test"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
