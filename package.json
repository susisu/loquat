{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "lint": "lerna run --ignore loquat --concurrency 1 --no-bail lint -- --color",
    "lint:fast": "lerna run --ignore loquat lint",
    "test": "lerna run --ignore loquat --concurrency 1 --no-bail test -- --colors",
    "test:fast": "lerna run --ignore loquat test"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
